{
  "name": "employee-management-system",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "Backend",
    "Frontend/frontend"
  ],
  "scripts": {
    "build": "npm --prefix Frontend/frontend run build",
    "dev:backend": "npm --prefix Backend run dev",
    "dev:frontend": "npm --prefix Frontend/frontend run dev",
    "lint": "npm --prefix Frontend/frontend run lint"
  }
}
